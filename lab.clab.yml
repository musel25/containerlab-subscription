
name: quickstart-lab
topology:
  nodes:
    # 1. The Router
    r1:
      kind: nokia_srlinux
      image: ghcr.io/nokia/srlinux:latest

    # 2. The Telemetry Collector
    gnmic:
      kind: linux
      image: ghcr.io/openconfig/gnmic:latest
      binds:
        - ./gnmic.yml:/gnmic.yml:ro
      cmd: --config /gnmic.yml subscribe

    # 3. The Time-Series Database
    prometheus:
      kind: linux
      image: prom/prometheus:latest
      binds:
        - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      ports:
        - "9090:9090"

    # 4. The Visualization Dashboard
    grafana:
      kind: linux
      image: grafana/grafana:latest
      ports:
        - "3000:3000"


# name: quickstart-lab
# topology:
#   nodes:
#     # 1. The Cisco XR Router (Replaces Nokia SR Linux)
#     xr1:
#       kind: cisco_xrd
#       # This image MUST exist in your local 'docker images' list. 
#       # It will not automatically download from the internet.
#       image: ios-xr-xrd-control-plane:7.9.2 
      
#       # Cisco XRd often requires a startup config to enable the gRPC server out of the box
#       # startup-config: xr-startup.cfg 

#     # 2. The Telemetry Collector
#     gnmic:
#       kind: linux
#       image: ghcr.io/openconfig/gnmic:latest
#       binds:
#         - ./gnmic.yml:/gnmic.yml:ro
#       cmd: --config /gnmic.yml subscribe

#     # 3. The Time-Series Database
#     prometheus:
#       kind: linux
#       image: prom/prometheus:latest
#       binds:
#         - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
#       ports:
#         - "9090:9090"

#     # 4. The Visualization Dashboard
#     grafana:
#       kind: linux
#       image: grafana/grafana:latest
#       ports:
#         - "3000:3000"

#   # Optional: Define standard links if you want to route traffic later
#   links:
#     - endpoints: ["xr1:eth1", "gnmic:eth1"]